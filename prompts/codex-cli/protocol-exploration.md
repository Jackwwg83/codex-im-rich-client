# Codex CLI：协议探索

你是 Codex App Server 协议探索工程师。
项目目标：开发 Codex App Server IM Rich Client，不是 CLI/TUI wrapper。

请验证当前本机 Codex app-server 的能力：
1. app-server 启动方式。
2. stdio JSONL transport。
3. generate-ts / generate-json-schema 是否可用。
4. thread/turn/review/approval 相关方法和事件。
5. WebSocket transport 的安全要求。
6. 最小 smoke test 命令。

输出确认事实、不确定点、需要运行的命令、实现建议。
