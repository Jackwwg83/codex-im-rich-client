VERDICT: APPROVE_WITH_CHANGES
SUMMARY: v2.2 is directionally close, but several v2.2 fixes are incomplete or internally inconsistent enough that T1 should not start until the P1s are patched.

P0 (blocks Phase 3 T1 implementation start):
  - none

P1 (required changes before T1):
  - [D41 / docs/superpowers/plans/2026-05-02-phase-3-plan.md:804,826,835,845; §16:1818-1824,1996-1997] D41 names new T18.1-T18.4 tasks for `InboundAction.rawCallbackData`, adapter JSDoc, boundary tests, and fake adapter behavior, but §16 never defines T18.1-T18.4 and already uses T18 for `Daemon.handleOnMessage` — the round-2b P0 boundary fix can be skipped in the TDD plan — add explicit T18.1-T18.4 tasks or renumber the daemon T18 task, and gate T16/T17 on those boundary tasks.
  - [D41 / docs/superpowers/plans/2026-05-02-phase-3-plan.md:805-821,1141-1155; packages/channel-core/src/adapter.ts:58-66; packages/channel-core/src/types.ts:102-108] The raw-token flow keeps Phase 2 `callbackNonce` fields required and documented as the broker-bound nonce, but production D33 binds a daemon-generated nonce before `sendCard` and decodes only `rawCallbackData` on inbound — this leaves the channel-core contract stale and can push implementers back toward bind-after-send semantics — amend `SendCardResult.callbackNonce` / `InboundAction.callbackNonce` docs and tests as legacy fallback-only when `wirePayload` is absent, or make the production nonce source explicit.
  - [§10.3 / docs/superpowers/plans/2026-05-02-phase-3-plan.md:1270-1275,1303-1306,1326-1336,1990-1991; packages/core/src/types.ts:290-299] The plan handles `duplicate_decision`, but Phase 2 broker exposes `already_resolved`, not `duplicate_decision` — T17.12 and the duplicate-click branches will not compile against the real broker API — replace every `duplicate_decision` reference with `already_resolved` and align the result-handling text with `priorDecision`.
  - [§10.3 / docs/superpowers/plans/2026-05-02-phase-3-plan.md:1263-1275; packages/core/src/approval-broker.ts:1002-1030] The `broker.resolve ok → SQLite CAS` order has an unsafe `rowsAffected === 0` branch: by the time CAS fails, `broker.resolve` has already mutated broker state and settled the wire response to Codex — the daemon cannot truthfully treat that as “already resolved” without acknowledging Codex already accepted the decision — add a pre-broker claim state/lease, or specify a recoverable post-ok storage failure path that revokes siblings and updates UI/audit consistently.
  - [§6/D29 / docs/superpowers/plans/2026-05-02-phase-3-plan.md:333-338,453-468; §10.3:1202-1267] The top-level redline and D29 step 10 still describe the old order: decode `action.callback_data`, CAS `bound→used`, then policy/broker; §10.3 says `rawCallbackData`, policy, `broker.resolve`, then CAS — this reintroduces the round-2b P1-2 confusion in the most prominent guardrail text — rewrite §6 and D29 step 10 to mirror §10.3 exactly.
  - [D42 / docs/superpowers/plans/2026-05-02-phase-3-plan.md:871-890; packages/codex-runtime/src/event-normalizer.ts:162-180,209-230,551-556] `endWithSynthetic` says to enqueue via `#enqueue` then call `endOfStream`, but `#enqueue` does not drain parked waiters; `#onNotification` drains separately, and `endOfStream` only resolves waiters when the queue is empty — pending consumers can hang with synthetics queued — specify `#enqueue` all synthetics, `#drain()`, then `endOfStream()`, with a test for a waiter already blocked on `.next()`.

P2 (nice-to-have):
  - [T0/§19 / docs/superpowers/plans/2026-05-02-phase-3-plan.md:1792-1799,2241,2299-2301; docs/phase-3/plan-v2-review-response.md:116] The v2.2 response claims stale v2/v2.1 references were fixed, but T0 and §19 still say “Plan v2”, “round 2”, and “Exit criteria (v2)” — update these to v2.2 / round 3.
  - [§10.2/§9 / docs/superpowers/plans/2026-05-02-phase-3-plan.md:1023-1043,1133-1139] §9 correctly says `actor_kind='im'` is set at INSERT, but §10.2 still sketches `actor: null` in the inserted record — align the record sketch with the schema columns.
  - [T29a / docs/superpowers/plans/2026-05-02-phase-3-plan.md:1678-1682,2122-2129] The Keychain wrapper does not leak the token via `security ... -w` argv, but the planned `--dry-run` “prints env” behavior can leak the token, and the wrapper does not state fail-closed behavior for missing/empty Keychain output — make dry-run redacted/source-only and require `set -euo pipefail` plus nonempty-token validation before `exec`.

NOTES:
  - Codex-R2-P0 is only partially fixed: D41 invokes D14 correctly and specifies `wirePayload` / `rawCallbackData` at lines 790-821, but the referenced T18.1-T18.4 tasks are absent from §16 and the old `callbackNonce` contract remains stale.
  - Codex-R2-P1-1 is fixed: schema uses `'abort'` at lines 997-998 and T6f covers all four action kinds at lines 1866-1870.
  - Codex-R2-P1-2 is partially fixed: §10.3 now keeps non-settling errors bound at lines 1202-1328, but §6/D29 still preserve the old CAS-before-broker order at lines 333-338 and 453-468.
  - Codex-R2-P1-3 is fixed in plan shape: D40 routes one approval through `#settleEntry` at lines 746-778, T6.5 sequences it before T17.x at lines 1810-1817, and T19e.4 calls the new single-approval API at lines 2059-2068.
  - Codex-R2-P1-4 is partially fixed: D42 and T19d use `turn_failed` and `endWithSynthetic` at lines 856-895 and 2009-2027, but the enqueue/drain behavior needs the P1 patch above.
  - Codex-R2-P1-5 is fixed: T15.5-T15.8 now cover D29 steps 1-13 and subscription ordering at lines 1921-1934.
  - Codex-R2-P2-1 is fixed at T19e.1 lines 2033-2048; P2-2 is mostly fixed at §9 lines 1023-1043 with one stale sketch; P2-3 is not fully fixed because stale version references remain.
  - No new implementation landed in `c606039`; the commit is docs-only. Project redlines remain directionally present in §3.3/§6/§22, but implementation should wait for the P1 plan corrections.
  - Cross-model alignment: I still agree with gstack round 2’s 0-P0 assessment on v2 at `docs/phase-3/plan-v2-gstack-round2-review.md:20-27`; this round’s issues are v2.2 integration and cross-section consistency defects, not a resurrection of the round-1 P0 class.
