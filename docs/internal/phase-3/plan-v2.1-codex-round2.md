VERDICT: REJECT
SUMMARY: v2.1 fixes the prior findings directionally, but the new opaque-token flow still has a missing adapter boundary and several v2.1 race/settlement details that must be patched before T1.

P0 (blocks Phase 3 T1 implementation start):
  - [§10.2/§10.3 / docs/superpowers/plans/2026-05-02-phase-3-plan.md:985-990,1041-1047; packages/render/src/approval-card.ts:34,69-80; packages/channel-core/src/adapter.ts:31-33,85-86; packages/channel-core/src/types.ts:102-116] D33/D34 requires the daemon to put per-action `"v1:" + rawToken` into callback_data and later decode `action.callback_data`, but the closed `ApprovalCard`/`ChannelAdapter`/`InboundAction` boundary has no outbound per-action callback payload field and no inbound raw callback_data/raw token field — this makes the central approval path unimplementable without violating the closed adapter boundary or falling back to the rejected v1 payload shape — add an explicit reviewed Phase 3 boundary amendment, e.g. per-action callback payloads on the rendered card plus `InboundAction.rawCallbackData`/`opaqueToken`, update fake + Telegram adapters, and add contract tests for both directions.

P1 (required changes before T1):
  - [§9 D34 / docs/superpowers/plans/2026-05-02-phase-3-plan.md:837-848; §10.3:1082-1088; packages/core/src/types.ts:94-98; packages/render/src/project-approval.ts:41-53] `callback_tokens.action` allows `cancel`, but the UI/broker action is `abort`; common rendered cards include `{kind:"abort"}` — this will either fail the schema CHECK at INSERT or pass a non-`ApprovalUiAction` to `broker.resolve` — change the stored action enum to `abort` or define an explicit DB-to-UI mapper, and test every visible action kind.
  - [§10.3 / docs/superpowers/plans/2026-05-02-phase-3-plan.md:1072-1081,1096-1103; packages/core/src/approval-broker.ts:941-944,954-979] The action handler marks a token `used` before `SecurityPolicy.checkUserAndChat` and before broker actor/target/nonce validation; wrong actors, wrong targets, and stale callbacks are broker validation errors that do not settle, so the token can be burned while the approval remains pending — move all non-settling validation before `bound→used`, or add a non-settling actor-policy validation/revert path; tests must assert wrong actor/target leave the token reusable or explicitly settled.
  - [§10.2/T19e.4 / docs/superpowers/plans/2026-05-02-phase-3-plan.md:1014-1021,1777-1782; packages/core/src/approval-broker.ts:1060-1065,1116-1123] The step-5 failure sweep depends on a “single approvalId transport_lost equivalent,” but the broker only exposes all-pending `failPendingAsTransportLost()` and age-based `expirePending()` — calling the existing method would settle unrelated approvals — specify and test a new single-approval settle API routed through the broker settle path.
  - [T19d / docs/superpowers/plans/2026-05-02-phase-3-plan.md:1736-1755; packages/codex-runtime/src/event-normalizer.ts:162-177; packages/codex-runtime/src/types.ts:46-53,84-85] G8 still has an ordering/type mismatch: it hooks the close path around `endOfStream`, but `endOfStream()` closes the iterator and ignores later events; it also says to use the `error` arm, which has no thread/turn ids, while `turn_failed` already exists — define an enqueue-before-close API or `endWithSynthetic(events)`, and synthesize `turn_failed` with thread/turn ids before `{done:true}`.
  - [D29/T15 / docs/superpowers/plans/2026-05-02-phase-3-plan.md:429-467,1676-1680] D29 requires `onPendingCreated`, `onAction`, and `onMessage` to be wired before `adapter.start`, but T15.4/T15.5 only name steps 8-9 and failure paths 1-9 — expand the init-order tests to cover D29 steps 8-12 and assert all subscriptions are installed before inbound events can arrive.

P2 (nice-to-have):
  - [T19e.1 / docs/superpowers/plans/2026-05-02-phase-3-plan.md:1756-1766; §11:1327-1330] T19e.1’s own test says concurrent click on a non-expired token, which does not prove the same-row expire-vs-click race; make the T19e.1 test explicitly race sweep and click on the same expired `bound` token.
  - [§9/§10.2 / docs/superpowers/plans/2026-05-02-phase-3-plan.md:863-872,955-967,1300-1305] §9 says `actor_kind='system'` for D36 auto-decline at INSERT, but §10.2 and the test say auto-decline sends no card and issues no token — remove the system-token wording or state it is schema headroom unused in Phase 3.
  - [§19/T0 / docs/superpowers/plans/2026-05-02-phase-3-plan.md:1580-1584,2013-2015] The review gate still says Codex round 2 on “v2” in a v2.1 plan — update to v2.1 so the exit criterion matches this review.

NOTES:
  - Round-1 P0 fixes are still present: pending-mode before supervisor start is in D29/D32 and tests at docs/superpowers/plans/2026-05-02-phase-3-plan.md:423-424,499-515,1283-1287; bind-before-send is at 318-323,541-557,1289-1298; auto-decline system actor is at 634-667,953-967,1300-1305; messageRef validation is at 606-632,1056-1071,1317-1320; attack rows T-Sec-11..14 are at 1137-1140,1322-1335; short opaque callback_data is at 471-486,858-861,1312-1315 and RequestId is unbounded at packages/codex-protocol/src/generated/RequestId.ts:5.
  - Round-2 P1s are integrated but not all fully sound: P1-A is addressed textually at 1006-1030,1141,1337-1345,1777-1782; P1-B at 1056-1066,1142,1347-1351,1813-1817; P1-C at 1138,1756-1766; P1-D at 1736-1782.
  - v2.1 introduced new structural risks: the opaque-token adapter boundary is missing, the pre-auth CAS can burn tokens, the single-approval transport-lost API does not exist, and G8 synthetic delivery is still order/type ambiguous.
  - Implementation should not begin until the P0/P1 items are patched and re-reviewed; T0.5/R6 still separately requires the protocol rebase at docs/superpowers/plans/2026-05-02-phase-3-plan.md:1581-1584,2187-2190.
  - Cross-model alignment: I agree with gstack that v2 fixed the round-1 P0 class and that v2.1 added concrete edits for the four round-2 P1s, as recorded in docs/phase-3/plan-v2-gstack-round2-review.md:20-27 and docs/phase-3/plan-v2-review-response.md:83-86; I diverge because the v2.1 edits expose new boundary and race defects.
