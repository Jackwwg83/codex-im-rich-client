{
  "codexVersion": "0.125.0",
  "platform": "darwin",
  "platformOs": "macos",
  "platformFamily": "unix",
  "machine": "arm64",
  "capturedAt": "2026-04-29",
  "capturedBy": "Phase 0 Section A Tasks 0.1–0.4",
  "experimentalFlag": false,
  "spikeMethod": "node spike.mjs (single-shot stdin write, hold for ≤5s waiting for first response line)",
  "wireObservations": {
    "jsonRpcVersionField": "absent — codex app-server uses JSON-RPC lite without the `jsonrpc` envelope field",
    "idType": "echoes whatever client sent — observed both number (case 1) and string (case 2) work; client should default to monotonic number for outgoing requests",
    "errorDataField": "absent in 0.125.0 — both unknown-method and invalid-params errors return only {code, message}",
    "errorCodeForUnknownMethod": -32600,
    "errorCodeForInvalidParams": -32600,
    "errorCodeCollision": "Codex 0.125.0 collapses unknown-method and invalid-params into the same -32600 code. Client must parse error.message to distinguish — keyword `unknown variant` for unknown-method vs `missing field`/`invalid type` for invalid-params.",
    "malformedJsonBehavior": "no JSON-RPC error response is sent on stdout. Codex writes a tracing log line to stderr with ANSI color codes. Client must treat stderr as plaintext, never JSON.",
    "initializeResponseShape": "{ userAgent, codexHome, platformFamily, platformOs } — note `platformFamily` and `platformOs` are SPLIT, not a single `platform` field as 05-PROTOCOL.md older drafts may suggest"
  },
  "fixtures": {
    "initialize-response.jsonl": "Case 1: numeric id initialize round-trip. Single response line.",
    "string-id-initialize-response.jsonl": "Case 2: string id initialize. Confirms server echoes string IDs verbatim.",
    "unknown-method-error.jsonl": "Case 3: unknown method `does/not/exist`. Returns -32600 with full method registry enumerated in error.message.",
    "invalid-params-error.jsonl": "Case 4: initialize with wrong params shape. Returns -32600 + `missing field clientInfo`.",
    "malformed-json.stderr.txt": "Case 5: non-JSON input. Empty stdout, ANSI-decorated tracing log on stderr.",
    "phase1-richer-turn-event-stream.jsonl": "Phase 1 T4 capture: 25 notifications across 11 distinct methods from a single richer-prompt turn (sandbox=read-only + approval_policy=on-request + default-reject). Includes turn lifecycle (turn/started, turn/completed), thread lifecycle (thread/started, thread/status/changed ×4), item lifecycle (item/started ×4, item/completed ×5), agent_message_delta, mcpServer/startupStatus/updated ×4, thread/tokenUsage/updated ×2, serverRequest/resolved, warning. Captured 2026-04-30 against codex 0.125.0 on Node 25.6.1; absolute paths and any model names redacted to <CWD> / <MODEL>; account/rateLimits/updated frames stripped (codex T4.5 review #2 — account metadata leak guard). The full method classification table for T7a still must cover account/rateLimits/updated; this fixture just doesn't exercise it.",
    "phase1-richer-turn-server-request.jsonl": "Phase 1 T4 capture: 1 server-initiated approval frame (item/fileChange/requestApproval, id=0). Used by T4.5's verify-phase1-fixtures.mts gate to assert the approval-capable method set has ≥1 representative for ApprovalBroker T9a contract tests."
  },
  "notes": [
    "id type observed: number (case 1) AND string (case 2) both accepted",
    "jsonrpc field observed: absent — wire is JSON-RPC lite",
    "error.data observed: absent on 0.125.0 (both -32600 cases lack data)",
    "malformed JSON: no JSON-RPC error response, stderr only (with ANSI color codes)",
    "approval method names from old 05-PROTOCOL.md are stale; the real names are item/{commandExecution,fileChange,permissions,tool}/{requestApproval,requestUserInput} on the server-initiated side, plus thread/approveGuardianDeniedAction on the client side",
    "codex 0.125.0 client method registry leaked verbatim in case-3 error.message — usable as a runtime sanity probe",
    "InitializeResult shape uses platformFamily + platformOs split, not single platform field",
    "Phase 1 T4 capture: a file-create prompt under sandbox=read-only + approval_policy=on-request reliably emits item/fileChange/requestApproval. The wire shape is { method, id, params: { threadId, turnId, itemId, reason, grantRoot } } — note `reason` and `grantRoot` are nullable, both observed null on this capture. Codex 0.125 logs `patch rejected by user` to stderr after the client default-rejects; the turn still reaches turn/completed cleanly with status=completed and items=[].",
    "Phase 1 T4 capture: codex 0.125 emits warning frames containing absolute paths in their message body (e.g. config.toml location). The redact-fixture.mjs script uses inline substring match (not whole-string) to scrub these — see scripts/redact-fixture.test.mjs regression test 'redacts paths embedded in prose'."
  ],
  "supersedes": null
}
